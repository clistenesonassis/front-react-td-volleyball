// eslint-disable-next-line no-shadow
export enum ReducerTypes {
  changeState = '@app/changeState',
  answer = '@app/answer',
  setPlaylist = '@app/setPlaylist',
  clearAnswer = '@app/clearAnswer',
  reset = '@app/reset',
}
